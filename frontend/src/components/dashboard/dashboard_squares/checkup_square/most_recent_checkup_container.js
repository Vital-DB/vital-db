import { connect } from 'react-redux';
import MostRecentCheckup from './most_recent_checkup';

const mapStateToProps = state => {
    return {

    }
}

const mapDispatchToProps = dispatch => {
    return {

    }
}

export default connect(mapStateToProps, mapDispatchToProps)(MostRecentCheckup);