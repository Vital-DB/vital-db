import { connect } from 'react-redux';
import PrescriptionSquare from './prescription_square';

const mapStateToProps = state => {
    return {

    }
}

const mapDispatchToProps = dispatch => {
    return {

    }
}

export default connect(mapStateToProps, mapDispatchToProps)(PrescriptionSquare);